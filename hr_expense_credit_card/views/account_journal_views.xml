<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista heredada para agregar el campo is_support_document en account.journal -->
    <record id="view_account_journal_form_inherit" model="ir.ui.view">
        <field name="name">account.journal.form.inherit</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <!-- Agregar el campo después de la sección de contabilidad -->
            <xpath expr="//group[@name='bank_account_number']" position="after">
                <group name="support_document" string="Documento Soporte">
                    <field name="is_support_document"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
