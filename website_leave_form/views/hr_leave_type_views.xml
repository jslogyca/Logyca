<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extender vista de formulario de hr.leave.type -->
    <record id="hr_leave_type_view_form_inherit" model="ir.ui.view">
        <field name="name">hr.leave.type.form.inherit</field>
        <field name="model">hr.leave.type</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_status_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='leave_validation']" position="after">
                <group string="Configuración Web">
                    <field name="require_attachment"/>
                    <field name="one_day_per_semester"/>
                    <field name="notify_talent_culture"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
